# @mdsrs/cli

Command-line tools for Markdown-native spaced repetition collections.

## Commands

```sh
mdsrs check ./cards
mdsrs export ./cards
mdsrs export ./cards --pretty
```

`check` loads a collection and prints a short summary. `export` emits the parsed
collection as JSON for other tools.
